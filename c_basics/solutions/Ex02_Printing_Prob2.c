#include <stdio.h>

int main(void) {
  // Trailing \n removed below. Output will not include a new line character.
  // The consequence on screen is that the cursor will not move to a new
  // line after printing "Hello, World!"
  printf("Hello, World!");
  return 0;
}
